import 'package:flutter/material.dart';

// Colors
const Color kTextColor = Color(0xFFF8F8FF); // White text color
const Color kAppBarColor =
    Color(0xFF4CAF50); // Slightly green color for the AppBar background
const Color kButtonColor =
    Color(0xFF03DAC6); // A teal color for buttons (unchanged)

// Font Sizes
const double kTitleFontSize = 22.0;
const double kBodyFontSize = 16.0;
const double kSmallFontSize = 12.0;

// Padding
const double kDefaultPadding = 16.0;
